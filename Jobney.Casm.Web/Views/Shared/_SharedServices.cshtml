<script>
    (function () {
        'use strict';

        var app = angular.module('Jobney.Casm.SharedServices', []);

        app.config([function () {

        }]);

        app.run([function () {

        }]);

        app.constant('ServiceRoutes', {
            trip: {
                getById: '@Url.Action("GetById", "Trip")',
                reorderWaypoint: '@Url.Action("ReorderWaypoint", "Trip")',
                createWaypoint: '@Url.Action("CreateWaypoint", "Trip")'
            },
            pilot: {
                getById: '@Url.Action("GetById", "Pilot")' + '/',
                query: '@Url.Action("Query", "Pilot")' + '/',
                save: '@Url.Action("Save", "Pilot")' + '/'
            },
            passenger: {
                getById: '@Url.Action("GetById", "Passenger")' + '/',
                query: '@Url.Action("Query", "Passenger")' + '/',
                save: '@Url.Action("Save", "Passenger")' + '/'
            }
        });
    })();
</script>

@Scripts.Render("~/bundles/ng-shared-services")